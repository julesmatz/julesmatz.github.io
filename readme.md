# Notes
Personal site with my notes.

## Installation
Create a python virtual environment
```
python3 -m venv venv            # create venv in current folder
source venv/bin/activate        # activate venv (Linux)
pip install -r requirements.txt # install dependencies
```
Then look into the note Tools/website.md for how to build/deploy the site.

